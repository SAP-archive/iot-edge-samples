<mvc:View
    controllerName="dep.fiori.goodsissue.app.list"
    class="dep-fiori"
    displayBlock="true"
    height="100%"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:fb="sap.ui.comp.filterbar"
    xmlns:c="sap.ui.core"
    xmlns="sap.m">
    <Page
        showHeader="false"
        showSubHeader="false"
        enableScrolling="false">
        <VBox
            fitContainer="true"
            renderType="Bare">
            <c:Fragment fragmentName="dep.fiori.lib.frag.filterBar" type="XML" />
            <ScrollContainer
                vertical="true"
                height="100%"
                width="100%">
                <Table
                    id="list"
                    noDataText="{i18n>WorkorderList.empty}"
                    items="{odata>/Workorder_Goods_Issue}"
                    growing="true"
                    growingScrollToLoad="true"
                    mode="SingleSelectMaster"
                    itemPress="onItemPress">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>WorkorderList.title}" />
                            <ToolbarSpacer />
                            <Button
                                tooltip="{i18nGlobal>General.sortList}"
                                icon="sap-icon://drop-down-list"
                                press="onSortPress" />
                        </Toolbar>
                    </headerToolbar>
                    <columns>
                        <Column
                            vAlign="Middle"
                            width="25%">
                            <Title
                                text="{i18nGlobal>General.workorder}"
                                tooltip="{i18nGlobal>General.workorder}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            width="auto">
                            <Title
                                text="{i18nGlobal>General.description}"
                                tooltip="{i18nGlobal>General.description}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            width="25%">
                            <Title
                                text="{i18n>Workorder.systemStatus}"
                                tooltip="{i18n>Workorder.systemStatus}" />
                        </Column>
                    </columns>
                    <items>
                        <ColumnListItem type="Active">
                            <cells>
                                <Label
                                    text="{odata>AUFNR}"
                                    tooltip="{odata>AUFNR}" />
                                <Label
                                    text="{odata>KTEXT}"
                                    tooltip="{odata>KTEXT}" />
                                <Label text="{odata>SYSTEM_STATUS}" />
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </ScrollContainer>
        </VBox>
    </Page>
</mvc:View>