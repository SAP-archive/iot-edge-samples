<mvc:View
    controllerName="dep.fiori.manifest.app.list"
    class="dep-fiori"
    displayBlock="true"
    height="100%"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:fb="sap.ui.comp.filterbar"
    xmlns:c="sap.ui.core"
    xmlns="sap.m">
    <Page
        showHeader="false"
        showSubHeader="false"
        enableScrolling="false">
        <VBox
            fitContainer="true"
            renderType="Bare">
            <c:Fragment fragmentName="dep.fiori.lib.frag.filterBar" type="XML" />
            <ScrollContainer
                vertical="true"
                height="100%"
                width="100%">
                <Table
                    id="list"
                    items="{odata>/Manifest_List}"
                    noDataText="{i18nGlobal>General.listEmpty}"
                    growing="true"
                    growingScrollToLoad="true"
                    mode="SingleSelectMaster"
                    itemPress="onItemPress">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>Manifest.title}" />
                            <ToolbarSpacer />
                            <Button
                                tooltip="{i18nGlobal>General.sortList}"
                                icon="sap-icon://drop-down-list"
                                press="onSortPress" />
                        </Toolbar>
                    </headerToolbar>
                    <columns>
                        <Column 
                            vAlign="Middle">
                            <Title
                                text="{i18n>Manifest.id}"
                                tooltip="{i18n>Manifest.id}" />
                        </Column>
                        <Column 
                            vAlign="Middle">
                            <Title
                                text="{i18n>Container.id}"
                                tooltip="{i18n>Container.id}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Tablet"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title 
                                text="{i18n>Container.status}" 
                                tooltip="{i18n>Container.status}" />
                        </Column>
                        <Column 
                        	vAlign="Middle"
                            minScreenWidth="Tablet"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title 
                                text="{i18n>Container.sourceLoc}"
                                tooltip="{i18n>Container.sourceLoc}" />
                        </Column>
                        <Column 
                            vAlign="Middle"
                            minScreenWidth="Desktop"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title 
                                text="{i18n>Container.targetLoc}" 
                                tooltip="{i18n>Container.targetLoc}" />
                        </Column>
                    </columns>
                    <items>
                        <ColumnListItem type ="Active">
                            <cells>
                                <Label
                                    text="{odata>MANIFEST_ID}"
                                    tooltip="{odata>MANIFEST_ID}"
                                    design="Bold"
                                    class="identifierTextBlue" />
                                <Label
                                    text="{odata>CONTAINER_ID}"
                                    tooltip="{odata>CONTAINER_ID}" />
                                <Label
                                    text="{odata>CONTAINER_STATUS}"
                                    tooltip="{odata>CONTAINER_STATUS}" />
                                <Label
                                    text="{odata>SOURCE_LOC}"
                                    tooltip="{odata>SOURCE_LOC}" />
                                <Label
                                    text="{odata>TARGET_LOC}"
                                    tooltip="{odata>TARGET_LOC}" />
                            </cells>
                        </ColumnListItem>
                     </items>
                </Table>
            </ScrollContainer>
        </VBox>
    </Page>
</mvc:View>