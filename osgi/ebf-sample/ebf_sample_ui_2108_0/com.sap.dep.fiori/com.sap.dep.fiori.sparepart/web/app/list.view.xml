<mvc:View
    controllerName="dep.fiori.sparepart.app.list"
    class="dep-fiori"
    displayBlock="true"
    height="100%"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:fb="sap.ui.comp.filterbar"
    xmlns:c="sap.ui.core"
    xmlns="sap.m">
    <Page
        showHeader="false"
        showSubHeader="false"
        enableScrolling="false">
        <VBox
            fitContainer="true"
            renderType="Bare">
            <c:Fragment fragmentName="dep.fiori.lib.frag.filterBar" type="XML" />
            <ScrollContainer
                vertical="true"
                height="100%"
                width="100%">
                <Table
                    id="list"
                    items="{odata>/Spare_Part}"
                    noDataText="{i18n>List.listEmpty}"
                    growing="true"
                    growingScrollToLoad="true"
                    itemPress="onItemPress">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>List.title}" />
                            <ToolbarSpacer />
                            <Button
                                tooltip="{i18nGlobal>General.sortList}"
                                icon="sap-icon://drop-down-list"
                                press="onSortPress" />
                        </Toolbar>
                    </headerToolbar>
                    <columns>
                        <Column vAlign="Middle">
                            <Title text="{i18n>Part.internalId}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Tablet"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18nGlobal>General.description}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Desktop"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18n>Part.subclass}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Tablet"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18n>Part.source}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Tablet"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18n>Part.manufacturer}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Desktop"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18n>Part.mfrPartNumber}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Desktop"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18n>Part.eanNumber}" />
                        </Column>
                        <Column
                            vAlign="Middle"
                            minScreenWidth="Desktop"
                            demandPopin="true"
                            popinDisplay="Inline">
                            <Title text="{i18n>Part.uom}" />
                        </Column>
                    </columns>
                    <ColumnListItem type="Active">
                        <Label
                            text="{odata>INTERNAL_ID}"
                            tooltip="{odata>INTERNAL_ID}"
                            design="Bold"
                            class="identifierTextBlue" />
                        <Label
                            text="{odata>DESCRIPTION}"
                            tooltip="{odata>DESCRIPTION}" />
                        <Label
                            text="{odata>SUBCLASS}"
                            tooltip="{odata>SUBCLASS}" />
                        <Label
                            text="{odata>SOURCE}"
                            tooltip="{odata>SOURCE}" />
                        <Label
                            text="{odata>MANUFACTURER}"
                            tooltip="{odata>MANUFACTURER}" />
                        <Label
                            text="{odata>MFR_PART_NUMBER}"
                            tooltip="{odata>MFR_PART_NUMBER}" />
                        <Label
                            text="{odata>EAN_NUMBER}"
                            tooltip="{odata>EAN_NUMBER}" />
                        <Label
                            text="{odata>UOM}"
                            tooltip="{odata>UOM}" />
                    </ColumnListItem>
                </Table>
            </ScrollContainer>
        </VBox>
    </Page>
</mvc:View>