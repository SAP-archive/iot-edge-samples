<mvc:View
    controllerName="dep.fiori.transaction.app.list"
    class="dep-fiori"
    displayBlock="true"
    height="100%"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:fb="sap.ui.comp.filterbar"
    xmlns:c="sap.ui.core"
    xmlns="sap.m">
    <Page
        showHeader="false"
        showSubHeader="false"
        enableScrolling="false">
        <VBox
            fitContainer="true"
            renderType="Bare">
            <c:Fragment fragmentName="dep.fiori.lib.frag.filterBar" type="XML" />
            <ScrollContainer
                vertical="true"
                height="100%"
                width="100%">
                <Table
                    id="list"
                    items="{odata>/Edge_Error}"
                    noDataText="{i18nGlobal>General.listEmpty}"
                    growing="true"
                    growingScrollToLoad="true"
                    itemPress="onItemPress">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="{i18n>List.title}" />
                            <ToolbarSpacer />
                            <Button
                                tooltip="{i18nGlobal>General.sortList}"
                                icon="sap-icon://drop-down-list"
                                press="onSortPress" />
                        </Toolbar>
                    </headerToolbar>
                    <columns>
                        <Column vAlign="Middle">
                            <Title text="{i18nGlobal>General.transaction}" />
                        </Column>
                        <Column vAlign="Middle">
                            <Title text="{i18n>List.message}" />
                        </Column>
                        <Column vAlign="Middle">
                            <Title text="{i18n>List.object}" />
                        </Column>
                    </columns>
                    <ColumnListItem type="Active">
                        <Label
                            text="{odata>TRANSID}"
                            tooltip="{odata>TRANSID}"
                            design="Bold"
                            class="identifierTextBlue" />
                        <Label
                            text="{odata>MESSAGE}"
                            tooltip="{odata>MESSAGE}" />
                        <Label
                            text="{odata>OBJID}"
                            tooltip="{odata>OBJID}" />
                    </ColumnListItem>
                </Table>
            </ScrollContainer>
        </VBox>
    </Page>
</mvc:View>